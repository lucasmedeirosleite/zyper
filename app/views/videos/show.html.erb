<div class="row">
  <div class="col s12">
    <h3 class="header"><%= @video.title %><h3>
    <%= render partial: 'videos/player', locals: { video: @video }%>
    <%= link_to videos_path, class: 'waves-effect waves-light btn' do %>
      <i class="material-icons left">cloud</i>back
    <% end %>
  </div>
</div>